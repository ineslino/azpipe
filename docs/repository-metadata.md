# GitHub metadata proposal

Status: **not applied**. Target from local origin: `github.com/ineslino/azpipe`.
Read-only GitHub check on 2026-09-05 confirmed: public repository, default branch `main`, description `Azure DevOps pipeline visibility and analysis CLI`, empty homepage and no topics. Local tracking default is `origin/main`. Recheck before any later publication.

- Description: Select, review and monitor Azure DevOps pipelines from a terminal UI, with offline demo and scriptable batches.
- Topics: `azure-devops`, `pipelines`, `tui`, `golang`, `cli`, `developer-tools`.
- Website: omit; no dedicated documentation site is configured.
- Social preview: optional manual reuse of an exported hero after reviewing platform dimensions.

GitHub supports description, homepage and topic editing through the [official CLI](https://cli.github.com/manual/gh_repo_edit), checked 2026-09-05. No changes to visibility, protections, default branch or metadata are authorized by local repository preparation.
